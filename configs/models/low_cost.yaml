schema_version: "1.0"
profile_id: low_cost
routing_policy_version: "1.0.0"
structured_extraction:
  provider: mock
  model: extraction-mock-1
  timeout_seconds: 45
reasoning:
  provider: mock
  model: extraction-mock-1
  timeout_seconds: 60
budgets:
  max_input_tokens_per_run: 40000
  max_cost_usd_per_run: 0.50
