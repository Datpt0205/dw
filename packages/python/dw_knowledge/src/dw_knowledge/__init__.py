"""Knowledge context: document ingestion, trusted filtered retrieval and
evidence contracts. The Qdrant adapter arrives in phase 2; the gateway is the
only place tenant/ACL filters are injected."""

__version__ = "0.1.0"
