"""Connector gateway: canonical contracts and adapters for Slack/Teams/Planner/
Jira plus a fully-working mock connector. External DTOs never leak inward —
each adapter maps provider models to the canonical contracts (ACL pattern)."""

__version__ = "0.1.0"
