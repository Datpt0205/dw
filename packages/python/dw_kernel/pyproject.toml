[project]
name = "dw-kernel"
version = "0.1.0"
description = "Shared kernel: universal primitives only (no frameworks, no business logic)"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling>=1.26"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/dw_kernel"]
