"""Shared kernel for the Digital Worker platform.

Contains only universal primitives (identifiers, clock/id ports, base domain
event, errors, result, money, pagination). No frameworks, no business logic.
"""

__version__ = "0.1.0"
